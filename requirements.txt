# 墨韵AI Python 后端依赖（FastAPI 推荐，Flask 仅 server.py 旧版用）
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
requests>=2.31.0
python-dotenv>=1.0.0

# 旧 server.py（可选 Flask 后端）— 已废弃，新代码用 src/ 下的 FastAPI
Flask==3.0.0
flask-cors>=4.0.0
anthropic>=0.18.0

# 测试
pytest>=8.0.0
httpx>=0.26.0
